using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SampleScriptA : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Adding a sample explanation
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
